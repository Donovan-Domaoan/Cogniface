import React from "react";

const FaceRecognition = ({ onButtonSubmit }) => {
    return (
        <div className='center'>
            <img alt='' src={regions} />
        </div>
    );
}

export default FaceRecognition;